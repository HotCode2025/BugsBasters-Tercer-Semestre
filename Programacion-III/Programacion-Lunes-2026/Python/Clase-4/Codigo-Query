-- Consulta información seleccionada
-- SELECT * FROM persona WHERE id_persona IN (1, 2, 3);

-- Insertamos un nuevo elemento a la tabla
INSERT INTO persona (nombre, apellido, email)
VALUES ('Susana', 'Lara', 'slara@mail.com');

-- Hacemos la consulta para ver toda la información de la tabla
SELECT * FROM persona;

-- Actualizamos un registro
-- UPDATE persona
-- SET nombre = 'Ivonne',
--     apellido = 'Esparza',
--     email = 'iesparza@mail.com'
-- WHERE id_persona = 1;

-- Eliminamos un registro
-- DELETE FROM persona WHERE id_persona = 3;
